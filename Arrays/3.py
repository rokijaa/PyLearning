# 3. Write a Python program to reverse the order of the items in the array.

array = [1, 3, 5, 7, 9]

newarray = str(array)
revarray = array.reverse()

print("The reverse of array " + newarray + " is " + str(array) )